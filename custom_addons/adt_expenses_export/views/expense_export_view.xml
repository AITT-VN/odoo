<odoo>
    <record id="action_export_selected_expenses" model="ir.actions.server">
        <field name="name">📋 Misa Export</field>
        <field name="model_id" ref="hr_expense.model_hr_expense"/>
        <field name="binding_model_id" ref="hr_expense.model_hr_expense"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">
            action = model.export_selected_expenses(records.ids)
        </field>
    </record>
</odoo>
